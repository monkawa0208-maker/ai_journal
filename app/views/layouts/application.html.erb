<!DOCTYPE html>
<html>
  <head>
    <title>AI Journal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/core@6.1.10/index.global.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/@fullcalendar/daygrid@6.1.10/index.global.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= content_for?(:body_class) ? yield(:body_class) : '' %>">
    <header class="app-header">
      <div class="header-container">
        <div class="header-logo">
          <h1><%= link_to "AI Journal", "/" %></h1>
        </div>
        <% if user_signed_in? %>
          <nav class="header-nav">
            <%= link_to "日記一覧", entries_path, class: "nav-link" %>
            <%= link_to "📚 MyDictionary", vocabularies_path, class: "nav-link" %>
          </nav>
        <% end %>
        <div class="header-user">
          <% if user_signed_in? %>
            <span class="user-greeting">ようこそ</span>
            <span class="user-name"><%= current_user.nickname %> さん</span>
            <div class="header-actions">
              <%= link_to '登録情報の編集', edit_user_registration_path, class: 'login-btn' %>
              <%= link_to 'ログアウト', destroy_user_session_path, class: 'logout-btn', data: { turbo_method: :delete } %>
              <%= link_to "NEW POST", new_entry_path, class: "button new-post" %>
            </div>
          <% else %>
            <div class="header-actions">
              <%= link_to 'ログイン', new_user_session_path, class: 'login-btn' %>
              <%= link_to '新規登録', new_user_registration_path, class: 'signup-btn' %>
            </div>
          <% end %>
        </div>
      </div>
    </header>

    <main>
      <%= yield %>
    </main>
  </body>
</html>
